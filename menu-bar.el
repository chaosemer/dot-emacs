;;; init/menu-bar.el --- Menu bar customizations  -*- lexical-binding: t; -*-

;;; Code:

;; No more needed!  Just bugs to fix.
;;
;; TODO: Make x-popup-menu on TTYs properly handle mouse input (it
;; treats a mouse-click as "select current entry")
